matrix:
  include:
    - dist: bionic
    - dist: xenial
    - dist: trusty
script:
  - /sbin/sysctl net.ipv6.conf.all.disable_ipv6=1
  - /sbin/sysctl net.ipv6.conf.default.disable_ipv6=1
  - cat /etc/hosts
